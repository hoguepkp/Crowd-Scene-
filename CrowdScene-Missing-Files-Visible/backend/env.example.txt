# Backend environment variables
PORT=4000
DECAY_HOURS=2

# Persist DB by mounting a Render Disk and pointing here (optional)
DB_PATH=./crowdscene.db

# Required for live venue search (enable Maps JavaScript API + Places API + billing)
GOOGLE_PLACES_API_KEY=YOUR_PLACES_API_KEY_HERE
